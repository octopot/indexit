name: Continuous delivery

on:
  workflow_dispatch:
    inputs:
      reason:
        description: The reason for dispatching it manually.
        type: string
        default: manual healthcheck
        required: true

  push:
    tags: [ 'v*' ]

permissions:
  contents: read

jobs:
  release:
    name: Releasing
    runs-on: ubuntu-24.04
    permissions:
      contents: write

    steps:
      - name: Checkout the repository
        uses: actions/checkout@v7.0.1
        with: { fetch-depth: 0 }
      - name: Set up Go environment
        uses: actions/setup-go@v7.0.0
        with:
          go-version: 1.27.x
          cache-dependency-path: |
            go.sum
            tools/go.sum
          check-latest: true

      - name: Install tools for release checks
        run: make tools
      - name: Run fast check and tests
        run: make fast-check test TIMEOUT=2m
      - name: Publish the release
        uses: goreleaser/goreleaser-action@v7.2.3
        env:
          GITHUB_TOKEN: ${{ secrets.GORELEASER_TOKEN }}
        with:
          distribution: goreleaser
          version: v2.18.2
          # four targets build in parallel by default; gotd needs ~5GB each,
          # which overruns the 16GB runner — see .goreleaser.yml builds
          args: ${{ startsWith(github.ref, 'refs/tags/v') && 'release --clean --parallelism 2' || 'release --snapshot --clean --skip=publish --parallelism 2' }}

  notify:
    name: Notifying
    needs: [ release ]
    runs-on: ubuntu-24.04
    if: failure() || success()

    steps:
      - name: Checkout the repository
        uses: actions/checkout@v7.0.1
        with: { fetch-depth: 0 }

      - name: Send notification
        uses: ./.github/actions/notify
        with:
          emoji: 🚀
          channel: ${{ secrets.SLACK_WEBHOOK }}
          success: ${{ ! contains(needs.*.result, 'failure') }}
